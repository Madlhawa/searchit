sudo apt update;
sudo apt -y upgrade;

sudo apt install git;
sudo apt install -y python3-pip;
sudo apt install build-essential libssl-dev libffi-dev python3-dev;
source .profile

pip install scrapy;
pip install flask;
git clone https://github.com/Madlhawa/searchit.git;